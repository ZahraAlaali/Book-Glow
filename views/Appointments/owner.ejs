 <link rel="stylesheet" href="/stylesheets/appointmentsShow.css"/>
 <link href="https://fonts.googleapis.com/css2?family=Abril+Fatface&family=Cardo:ital,wght@0,400;0,700;1,400&family=Cinzel:wght@400..900&display=swap" rel="stylesheet">

<%- include('../partials/_navbar') %>

<h1>Booked Appointments</h1>

<ul>
  <% appointments.forEach(appointment => { %>
  <% if (appointment.userId) { %>
  <li>
      <strong>Date:</strong> <%= appointment.dateTime.toLocaleString() %><br>
      <strong>Booked by:</strong> <%= appointment.userId.username %><br>
      <strong>Services:</strong>
      <ul>
        <% appointment.services.forEach(service => { %>
        <li><%= service.name %> - $<%= service.price %></li>
        <% }) %>
      </ul>
    </li>
    <% } %>
  <% }) %>
  </ul>
